// Generated by winbindgen — do not edit.
const win_core = @import("win-core");
const BOOL = win_core.BOOL;
const GUID = win_core.GUID;
const HRESULT = win_core.HRESULT;
const NTSTATUS = win_core.NTSTATUS;
const BOOLEAN = win_core.BOOLEAN;
const @"Windows.Win32.Foundation" = @import("Windows.Win32.Foundation.structs.zig");
const @"Windows.Win32.Graphics.Direct2D.Common" = @import("Windows.Win32.Graphics.Direct2D.Common.structs.zig");

pub const DWRITE_COLOR_F = extern struct {
    r: f32,
    g: f32,
    b: f32,
    a: f32,
};
pub const DWRITE_FONT_METRICS = extern struct {
    designUnitsPerEm: u16,
    ascent: u16,
    descent: u16,
    lineGap: i16,
    capHeight: u16,
    xHeight: u16,
    underlinePosition: i16,
    underlineThickness: u16,
    strikethroughPosition: i16,
    strikethroughThickness: u16,
};
pub const DWRITE_GLYPH_METRICS = extern struct {
    leftSideBearing: i32,
    advanceWidth: u32,
    rightSideBearing: i32,
    topSideBearing: i32,
    advanceHeight: u32,
    bottomSideBearing: i32,
    verticalOriginY: i32,
};
pub const DWRITE_GLYPH_OFFSET = extern struct {
    advanceOffset: f32,
    ascenderOffset: f32,
};
pub const DWRITE_MATRIX = extern struct {
    m11: f32,
    m12: f32,
    m21: f32,
    m22: f32,
    dx: f32,
    dy: f32,
};
pub const DWRITE_TEXT_RANGE = extern struct {
    startPosition: u32,
    length: u32,
};
pub const DWRITE_FONT_FEATURE = extern struct {
    nameTag: u32,
    parameter: u32,
};
pub const DWRITE_TYPOGRAPHIC_FEATURES = extern struct {
    features: *DWRITE_FONT_FEATURE,
    featureCount: u32,
};
pub const DWRITE_TRIMMING = extern struct {
    granularity: i32,
    delimiter: u32,
    delimiterCount: u32,
};
pub const DWRITE_SCRIPT_ANALYSIS = extern struct {
    script: u16,
    shapes: i32,
};
pub const DWRITE_LINE_BREAKPOINT = extern struct {
    _bitfield: u8,
};
pub const DWRITE_SHAPING_TEXT_PROPERTIES = extern struct {
    _bitfield: u16,
};
pub const DWRITE_SHAPING_GLYPH_PROPERTIES = extern struct {
    _bitfield: u16,
};
pub const DWRITE_GLYPH_RUN_DESCRIPTION = extern struct {
    localeName: @"Windows.Win32.Foundation".PWSTR,
    string: @"Windows.Win32.Foundation".PWSTR,
    stringLength: u32,
    clusterMap: *u16,
    textPosition: u32,
};
pub const DWRITE_UNDERLINE = extern struct {
    width: f32,
    thickness: f32,
    offset: f32,
    runHeight: f32,
    readingDirection: i32,
    flowDirection: i32,
    localeName: @"Windows.Win32.Foundation".PWSTR,
    measuringMode: i32,
};
pub const DWRITE_STRIKETHROUGH = extern struct {
    width: f32,
    thickness: f32,
    offset: f32,
    readingDirection: i32,
    flowDirection: i32,
    localeName: @"Windows.Win32.Foundation".PWSTR,
    measuringMode: i32,
};
pub const DWRITE_LINE_METRICS = extern struct {
    length: u32,
    trailingWhitespaceLength: u32,
    newlineLength: u32,
    height: f32,
    baseline: f32,
    isTrimmed: @"Windows.Win32.Foundation".BOOL,
};
pub const DWRITE_CLUSTER_METRICS = extern struct {
    width: f32,
    length: u16,
    _bitfield: u16,
};
pub const DWRITE_TEXT_METRICS = extern struct {
    left: f32,
    top: f32,
    width: f32,
    widthIncludingTrailingWhitespace: f32,
    height: f32,
    layoutWidth: f32,
    layoutHeight: f32,
    maxBidiReorderingDepth: u32,
    lineCount: u32,
};
pub const DWRITE_INLINE_OBJECT_METRICS = extern struct {
    width: f32,
    height: f32,
    baseline: f32,
    supportsSideways: @"Windows.Win32.Foundation".BOOL,
};
pub const DWRITE_OVERHANG_METRICS = extern struct {
    left: f32,
    top: f32,
    right: f32,
    bottom: f32,
};
pub const DWRITE_HIT_TEST_METRICS = extern struct {
    textPosition: u32,
    length: u32,
    left: f32,
    top: f32,
    width: f32,
    height: f32,
    bidiLevel: u32,
    isText: @"Windows.Win32.Foundation".BOOL,
    isTrimmed: @"Windows.Win32.Foundation".BOOL,
};
pub const DWRITE_FONT_METRICS1 = extern struct {
    Base: DWRITE_FONT_METRICS,
    glyphBoxLeft: i16,
    glyphBoxTop: i16,
    glyphBoxRight: i16,
    glyphBoxBottom: i16,
    subscriptPositionX: i16,
    subscriptPositionY: i16,
    subscriptSizeX: i16,
    subscriptSizeY: i16,
    superscriptPositionX: i16,
    superscriptPositionY: i16,
    superscriptSizeX: i16,
    superscriptSizeY: i16,
    hasTypographicMetrics: @"Windows.Win32.Foundation".BOOL,
};
pub const DWRITE_CARET_METRICS = extern struct {
    slopeRise: i16,
    slopeRun: i16,
    offset: i16,
};
pub const DWRITE_PANOSE = extern union {
    pub const _text_e__Struct = extern struct {
        familyKind: u8,
        serifStyle: u8,
        weight: u8,
        proportion: u8,
        contrast: u8,
        strokeVariation: u8,
        armStyle: u8,
        letterform: u8,
        midline: u8,
        xHeight: u8,
    };
    pub const _script_e__Struct = extern struct {
        familyKind: u8,
        toolKind: u8,
        weight: u8,
        spacing: u8,
        aspectRatio: u8,
        contrast: u8,
        scriptTopology: u8,
        scriptForm: u8,
        finials: u8,
        xAscent: u8,
    };
    pub const _decorative_e__Struct = extern struct {
        familyKind: u8,
        decorativeClass: u8,
        weight: u8,
        aspect: u8,
        contrast: u8,
        serifVariant: u8,
        fill: u8,
        lining: u8,
        decorativeTopology: u8,
        characterRange: u8,
    };
    pub const _symbol_e__Struct = extern struct {
        familyKind: u8,
        symbolKind: u8,
        weight: u8,
        spacing: u8,
        aspectRatioAndContrast: u8,
        aspectRatio94: u8,
        aspectRatio119: u8,
        aspectRatio157: u8,
        aspectRatio163: u8,
        aspectRatio211: u8,
    };
    values: [10]u8,
    familyKind: u8,
    text: _text_e__Struct,
    script: _script_e__Struct,
    decorative: _decorative_e__Struct,
    symbol: _symbol_e__Struct,
};
pub const DWRITE_UNICODE_RANGE = extern struct {
    first: u32,
    last: u32,
};
pub const DWRITE_SCRIPT_PROPERTIES = extern struct {
    isoScriptCode: u32,
    isoScriptNumber: u32,
    clusterLookahead: u32,
    justificationCharacter: u32,
    _bitfield: u32,
};
pub const DWRITE_JUSTIFICATION_OPPORTUNITY = extern struct {
    expansionMinimum: f32,
    expansionMaximum: f32,
    compressionMaximum: f32,
    _bitfield: u32,
};
pub const DWRITE_TEXT_METRICS1 = extern struct {
    Base: DWRITE_TEXT_METRICS,
    heightIncludingTrailingWhitespace: f32,
};
pub const DWRITE_FONT_PROPERTY = extern struct {
    propertyId: i32,
    propertyValue: @"Windows.Win32.Foundation".PWSTR,
    localeName: @"Windows.Win32.Foundation".PWSTR,
};
pub const DWRITE_LINE_METRICS1 = extern struct {
    Base: DWRITE_LINE_METRICS,
    leadingBefore: f32,
    leadingAfter: f32,
};
pub const DWRITE_LINE_SPACING = extern struct {
    method: i32,
    height: f32,
    baseline: f32,
    leadingBefore: f32,
    fontLineGapUsage: i32,
};
pub const DWRITE_GLYPH_IMAGE_DATA = extern struct {
    imageData: *anyopaque,
    imageDataSize: u32,
    uniqueDataId: u32,
    pixelsPerEm: u32,
    pixelSize: @"Windows.Win32.Graphics.Direct2D.Common".D2D_SIZE_U,
    horizontalLeftOrigin: @"Windows.Win32.Foundation".POINT,
    horizontalRightOrigin: @"Windows.Win32.Foundation".POINT,
    verticalTopOrigin: @"Windows.Win32.Foundation".POINT,
    verticalBottomOrigin: @"Windows.Win32.Foundation".POINT,
};
pub const DWRITE_FILE_FRAGMENT = extern struct {
    fileOffset: u64,
    fragmentSize: u64,
};
pub const DWRITE_FONT_AXIS_VALUE = extern struct {
    axisTag: u32,
    value: f32,
};
pub const DWRITE_FONT_AXIS_RANGE = extern struct {
    axisTag: u32,
    minValue: f32,
    maxValue: f32,
};
pub const DWRITE_BITMAP_DATA_BGRA32 = extern struct {
    width: u32,
    height: u32,
    pixels: *u32,
};
pub const DWRITE_PAINT_COLOR = extern struct {
    value: DWRITE_COLOR_F,
    paletteEntryIndex: u16,
    alphaMultiplier: f32,
    colorAttributes: i32,
};
pub const DWRITE_PAINT_ELEMENT = extern struct {
    pub const PAINT_UNION = extern union {
        pub const PAINT_LAYERS = extern struct {
            childCount: u32,
        };
        pub const PAINT_SOLID_GLYPH = extern struct {
            glyphIndex: u32,
            color: DWRITE_PAINT_COLOR,
        };
        pub const PAINT_LINEAR_GRADIENT = extern struct {
            extendMode: u32,
            gradientStopCount: u32,
            x0: f32,
            y0: f32,
            x1: f32,
            y1: f32,
            x2: f32,
            y2: f32,
        };
        pub const PAINT_RADIAL_GRADIENT = extern struct {
            extendMode: u32,
            gradientStopCount: u32,
            x0: f32,
            y0: f32,
            radius0: f32,
            x1: f32,
            y1: f32,
            radius1: f32,
        };
        pub const PAINT_SWEEP_GRADIENT = extern struct {
            extendMode: u32,
            gradientStopCount: u32,
            centerX: f32,
            centerY: f32,
            startAngle: f32,
            endAngle: f32,
        };
        pub const PAINT_GLYPH = extern struct {
            glyphIndex: u32,
        };
        pub const PAINT_COLOR_GLYPH = extern struct {
            glyphIndex: u32,
            clipBox: @"Windows.Win32.Graphics.Direct2D.Common".D2D_RECT_F,
        };
        pub const PAINT_COMPOSITE = extern struct {
            mode: i32,
        };
        layers: PAINT_LAYERS,
        solidGlyph: PAINT_SOLID_GLYPH,
        solid: DWRITE_PAINT_COLOR,
        linearGradient: PAINT_LINEAR_GRADIENT,
        radialGradient: PAINT_RADIAL_GRADIENT,
        sweepGradient: PAINT_SWEEP_GRADIENT,
        glyph: PAINT_GLYPH,
        colorGlyph: PAINT_COLOR_GLYPH,
        transform: DWRITE_MATRIX,
        composite: PAINT_COMPOSITE,
    };
    paintType: i32,
    paint: PAINT_UNION,
};
